<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - ระบบลงทะเบียนเรียน</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">📚 ระบบลงทะเบียนเรียน</a>
            <div class="navbar-nav ms-auto">
                <span class="navbar-text me-3">สวัสดี, <%= user.fullName %> (<%= user.role %>)</span>
                <a class="btn btn-outline-light btn-sm" href="/logout">ออกจากระบบ</a>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        <div class="row">
            <div class="col-md-3">
                <div class="card">
                    <div class="card-header">
                        <h6>📋 เมนูนักเรียน</h6>
                    </div>
                    <div class="list-group list-group-flush">
                        <a href="/dashboard" class="list-group-item list-group-item-action">
                            🏠 หน้าหลัก
                        </a>
                        <a href="/student/profile" class="list-group-item list-group-item-action active">
                            👤 ข้อมูลส่วนตัว
                        </a>
                        <a href="/student/register" class="list-group-item list-group-item-action">
                            📝 ลงทะเบียนเรียน
                        </a>
                        <a href="/student/grades" class="list-group-item list-group-item-action">
                            📊 ผลการเรียน
                        </a>
                    </div>
                </div>
            </div>
            
            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">
                        <h5>👤 <%= title %></h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-8">
                                <table class="table table-bordered">
                                    <tr>
                                        <td width="30%" class="fw-bold bg-light">รหัสนักเรียน</td>
                                        <td><%= student.studentId %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold bg-light">ชื่อ-นามสกุล</td>
                                        <td><%= student.fullName %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold bg-light">ชื่อเล่น</td>
                                        <td><%= student.nickname || '-' %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold bg-light">อีเมล</td>
                                        <td><%= student.email || '-' %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold bg-light">เบอร์โทร</td>
                                        <td><%= student.phone || '-' %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold bg-light">หลักสูตร</td>
                                        <td><%= student.curriculumId %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold bg-light">ชั้นปี</td>
                                        <td><%= student.year %></td>
                                    </tr>
                                    <tr>
                                        <td class="fw-bold bg-light">สถานะ</td>
                                        <td>
                                            <% if (student.status === 'active') { %>
                                                <span class="badge bg-success">กำลังศึกษา</span>
                                            <% } else if (student.status === 'graduated') { %>
                                                <span class="badge bg-primary">จบการศึกษา</span>
                                            <% } else { %>
                                                <span class="badge bg-secondary">ไม่ระบุ</span>
                                            <% } %>
                                        </td>
                                    </tr>
                                </table>
                            </div>
                            <div class="col-md-4">
                                <div class="card">
                                    <div class="card-header">
                                        <h6>📊 สถิติการเรียน</h6>
                                    </div>
                                    <div class="card-body">
                                        <div class="text-center mb-3">
                                            <h4 class="text-primary"><%= totalCredits %></h4>
                                            <small>หน่วยกิตที่ลงทะเบียน</small>
                                        </div>
                                        <div class="text-center mb-3">
                                            <h4 class="text-success"><%= registeredSubjects.length %></h4>
                                            <small>วิชาที่ลงทะเบียน</small>
                                        </div>
                                        <div class="progress mb-2">
                                            <div class="progress-bar" role="progressbar" 
                                                 style="width: <%= Math.min((totalCredits/120)*100, 100) %>%">
                                                <%= Math.round((totalCredits/120)*100) %>%
                                            </div>
                                        </div>
                                        <small class="text-muted">ความคืบหน้าการศึกษา (จาก 120 หน่วยกิต)</small>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- ตารางวิชาที่ลงทะเบียนถูกลบตามคำขอ -->
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-light text-center py-3 mt-5">
        <div class="container">
            <p class="mb-0">&copy; 2024 ระบบลงทะเบียนเรียน - MVC Architecture</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>